node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store
.gstack/

# Media and databases live outside git: they are large and regenerable.
*.mp4
*.srt
.superpowers/

# Secrets: only the example is tracked.
.env

# Pipeline working set: audio, transcripts and whisper models are large and regenerable.
pipeline/work/
pipeline/models/
*.wav

__pycache__/
_workspace/

# iCloud drops these next to the original; they break Android resource parsing.
* [0-9].*

# 발표용 PNG 한 벌. 슬라이드 HTML 에서 다시 렌더된다.
docs/부록-최종/
docs/부록-최종-라이트/
