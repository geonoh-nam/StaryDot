# 값을 채워 backend/.env 로 복사한다. .env 는 커밋하지 않는다.

# 콘텐츠 서버 포트. 비우면 5056.
STARY_PORT=

# 아이 스케치를 그림으로 바꾸는 보조 스크립트(backend/tools)에서 쓴다.
# Google AI Studio 에서 발급한다.
GEMINI_API_KEY=

# 파이프라인 API 경로에서만 쓴다. 에이전트 경로는 없이 돈다.
ANTHROPIC_API_KEY=
